import React from 'react';

const Limit = (props) => (
  <React.Fragment>
    <button className="show_more" onClick={props.showMore}>Show More...</button>
  </React.Fragment>
);

export default Limit;
